glossary = {.append(): 'adds an element to a list',
            'insert': 'inserts an element into a certain location in a list',
            'del'   deletes an element from a list
# LIST.pop()   deletes an element from a list, but keeps that element stored as a variable
# LIST.remove()   removes a value from a list}
print(glossary)